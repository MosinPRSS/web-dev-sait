# По поводу 2 и 3 работ

- Здесь используется шаблонизатор, поскольку некоторая часть кода будет переиспользована вновь и вновь, 
поэтому для читабельности и понимания того, что происходит, будет использоваться Flask и Jinja2 (Python).

- Однако позже при разработке с использованием NodeJS будут использованы React + Golang.
